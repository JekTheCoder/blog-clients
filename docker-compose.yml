version: '3'

services:
  blog:
    image: jekimager/blog-client
    environment:
      - API_URL=${API_URL}
    build:
      context: .
      dockerfile: Dockerfile.blog
      args:
        DOCKER_BUILDKIT: 1
    ports:
      - "80:4173"
